<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
    <!-- ==================== -->
    <!-- 加载Bean             -->
    <!-- ==================== -->
    <!-- 注册并激活 -->
    <context:component-scan base-package="study.spring.support"
                            use-default-filters="true">
    </context:component-scan>
    <!-- 激活 -->
    <context:annotation-config/>
    <!-- ==================== -->
    <!-- 加载配置文件          -->
    <!-- ==================== -->
    <!--<util:properties id="config" location="classpath*:*.properties"/>-->
    <!--<context:property-placeholder location="classpath*:*.properties" ignore-unresolvable="true"/>-->
    <!-- ==================== -->
    <!-- 扩展点                -->
    <!-- ==================== -->
    <bean class="study.spring.core.ext.MyBean" init-method="initMethod" destroy-method="destroyMethod">
        <property name="name" value="abc"/>
    </bean>
    <bean class="study.spring.core.ext.MyBeanPostProcessor" />
    <bean class="study.spring.core.ext.MyInstantiationAwareBeanPostProcessor" />
    <bean class="study.spring.core.ext.MyBeanFactoryPostProcessor" />
    <bean class="study.spring.core.ext.MyBeanDefinitionRegistryPostProcessor" />
</beans>